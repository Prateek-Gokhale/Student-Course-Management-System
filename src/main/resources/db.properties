# ============================================================
# SCMS - Database Configuration
# ============================================================
# Update these values to match your PostgreSQL setup

# Primary connection settings
db.url=jdbc:postgresql://localhost:5432/studentdb
db.username=postgres
db.password=yourpassword
db.driver=org.postgresql.Driver

# HikariCP Connection Pool settings
pool.maximumPoolSize=10
pool.minimumIdle=2
pool.connectionTimeout=30000
pool.idleTimeout=600000
pool.maxLifetime=1800000
pool.poolName=SCMS-HikariPool
